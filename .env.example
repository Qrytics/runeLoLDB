# PostgreSQL connection
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/runeloldb

# Optional: path to the League of Legends lockfile (auto-detected on most systems)
# LOL_LOCKFILE_PATH=C:/Riot Games/League of Legends/lockfile

# Backend URL used by the Electron frontend
BACKEND_URL=http://localhost:8000
